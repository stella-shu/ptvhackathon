VITE_API_BASE_URL=http://localhost:8080

# Optional: split targets to merge two backends behind Vite dev proxy
# Auth & Logs Backend (JWT, incidents, shifts)
AUTH_TARGET=http://localhost:8080
# Realtime Maps Backend (WebSocket + blitz)
REALTIME_TARGET=http://localhost:8081

# Note: Google Maps API key is no longer required — we switched to Leaflet + OpenStreetMap.
